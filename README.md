# Setup

1. Create venv:
   py -m venv .venv

2. Activate:
   .\.venv\Scripts\activate

3. Install:
   pip install -r requirements.txt

4. Place dataset locally:
   data/yelp_final.csv

5. Run:
   python Lexikon_Training_Topics.py
